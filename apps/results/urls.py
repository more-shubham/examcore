from django.urls import path

app_name = 'results'

urlpatterns = [
    # path('', views.ResultListView.as_view(), name='list'),
    # path('<int:pk>/', views.ResultDetailView.as_view(), name='detail'),
]
